{% extends 'base.html.twig' %}

{% block title %}Mes adresses - My French Boutique
{% endblock %}


{% block content %}

	<a href="{{ path('app_account_address_add') }}" class="btn btn-info btn-sm float-right">Ajouter une adresse</a>
	<h1>Mes adresses</h1>
	C'est dans cet espace que vous allez pouvoir gerer toutes vos adresses.
	<hr>
	<a href="{{ path('app_account') }}">Retour</a>

	{% if app.user.addresses | length == 0 %}
		<p class="text-center">
			Vous n' avez pas encore ajouté d' adresse dans votre compte client.<br>
			Pour en ajouter une, Veuillez
			<a href="{{ path('app_account_address_add') }}">Cliquez ici</a>
		</p>
	{% else %}
		<div class="row">
			{% for address in app.user.addresses %}
				<div class="col-md-2"></div>
				<div class="card" style="width: 18rem;">
					<div class="card-body">
						<h5 class="card-title">{{ address.name }}</h5>
						<p class="card-text">
							{{ address.address }},<br>
							{{ address.postal }} - {{ address.city }} - {{ address.country }}
						</p>
						<a href="{{ path('app_account_address_edit', {'id': address.id}) }}">Modifier</a> | 
						<a href="{{ path('app_account_address_delete', {'id' : address.id}) }}">Supprimer</a>
					</div>
				</div>
			{% endfor %}
		</div>
	{% endif %}


{% endblock %}
